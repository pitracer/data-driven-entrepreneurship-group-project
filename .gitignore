# Secrets
.env

# Data — pipeline inputs/intermediates: local only, never committed
data/raw/
data/processed/
# data/final/ is intentionally NOT ignored — app reads from there, committed to git

# Cache IS committed — teammates need it to avoid re-burning API credits
# data/cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
*.Zone.Identifier
